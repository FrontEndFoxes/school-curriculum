---
interface Props {
  id: number;
  title: string;
  lang: string;
}

const { id, title, lang } = Astro.props;
---

<a href={`/${lang}/quiz/${id}`} class="block">
  <div class="p-3">
    <p class="text-xl font-semibold text-gray-800 underline">{title}</p>
  </div>
</a>